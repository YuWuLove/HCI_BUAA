<%= form_for(@weibo, :url => weibos_path, :html => { :multipart => true }) do |f| %>
  <% if @weibo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weibo.errors.count, "error") %> prohibited this weibo from being saved:</h2>

      <ul>
      <% @weibo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
    <%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
